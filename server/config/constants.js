const host = "localhost:52313";
const path_prefix = "/api/";
const protocol = "http://";

module.exports.HOST = host;
module.exports.PATH_PREFIX = path_prefix
module.exports.PROTOCOL = protocol;

module.exports.URL_HOST = protocol + host + path_prefix;